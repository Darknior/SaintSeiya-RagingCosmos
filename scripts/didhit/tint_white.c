#import "data/scripts/didhit/main.h"

void main()
{
	tintEffect(openborconstant("ANI_IDLE"), "white", 1);
	tintEffect(openborconstant("ANI_RUNATTACK"), "white", 1);
	tintEffect(openborconstant("ANI_SPECIAL"), "white", 1);
	tintEffect(openborconstant("ANI_SPECIAL2"), "white", 1);
	tintEffect(openborconstant("ANI_FREESPECIAL"), "white", 1);
	tintEffect(openborconstant("ANI_FREESPECIAL2"), "white", 1);
	tintEffect(openborconstant("ANI_FREESPECIAL3"), "white", 1);
	tintEffect(openborconstant("ANI_FREESPECIAL6"), "white", 1);
	autoLand();
	rushFont();
}